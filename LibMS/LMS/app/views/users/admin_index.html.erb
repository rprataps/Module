<% provide(:title, 'All admins') %>
  <%= render 'shared/manage_admin_partial' %>
<p>All admins</p>
<%= will_paginate %>
<ul class="users">
  <% @users.each do |user| %>
    <% if user.user_type != "user" %>
      <%= render user %>
    <% end %>
  <% end %>
</ul>

<%= will_paginate %>
