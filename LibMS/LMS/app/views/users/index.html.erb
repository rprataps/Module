<% provide(:title, 'All members') %>
  <%= render 'shared/manage_member_partial' %>
<p>All Library Members</p>

<%= will_paginate %>

<ul class="users">
  <% @users.each do |user| %>
    <% if user.user_type == "user" %>
      <%= render user %>
    <% end %>
  <% end %>
</ul>

<%= will_paginate %>
